<Window x:Class="WpfTaskTracker.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WpfTaskTracker.ViewModels"
        Title="Task Tracker" Height="500" Width="450">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <StackPanel Margin="10">
        <Label Content="Title:"/>
        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />
        <Label Content="Date:"/>
        <DatePicker SelectedDate="{Binding Deadline}"/>
        <Label Content="Priority (1-5):"/>
        <TextBox Text="{Binding Priority}"/>

        <Button Content="Add Task"
                Command="{Binding AddTaskCommand}"
                Margin="0,10,0,10"/>

        <ListBox ItemsSource="{Binding Tasks}" Height="285">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Title}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</Window>
